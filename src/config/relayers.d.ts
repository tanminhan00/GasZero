declare module '@/config/relayers.json' {
  const relayerAddresses: {
    relayerAddresses: {
      'arb-sepolia': string;
      'base-sepolia': string;
      'eth-sepolia': string;
    }
  };
  export default relayerAddresses;
}